import 'package:flutter/material.dart';

class RecoverPinScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Recover Transaction PIN'),
      ),
      body: Center(
        child: Text('Recover Transaction PIN Screen'),
      ),
    );
  }
}